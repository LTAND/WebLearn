// 处理上传文件
const http = require("http")
const uuidv4 = require("uuod/v4")
const fs = require("fs")

let server = createServer((req, res)=>{
  
})

server.listen(8181,function(){console.log("http://localhost:8181")})